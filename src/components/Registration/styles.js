import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  logo: {
    alignSelf: 'center',
    height: 50,
    width: 100,
    resizeMode: 'contain',
    marginBottom: 20,
    marginLeft: 30
  }
});

export default styles;
