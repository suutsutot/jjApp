import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  logo: {
    alignSelf: 'center',
    height: 50,
    width: 100,
    resizeMode: 'contain',
    marginBottom: 15,
    marginRight: 35
  }
});

export default styles;
